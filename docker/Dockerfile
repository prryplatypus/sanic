ARG BASE_IMAGE_ORG
ARG BASE_IMAGE_NAME
ARG BASE_IMAGE_TAG

FROM ${BASE_IMAGE_ORG}/${BASE_IMAGE_NAME}:${BASE_IMAGE_TAG}

RUN apk update
RUN update-ca-certificates

ARG SANIC_PYPI_VERSION
ARG INDEX_URL

RUN pip install --index-url ${INDEX_URL} sanic==${SANIC_PYPI_VERSION}
RUN apk del build-base
